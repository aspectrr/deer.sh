filter {
  if "weather_parsed" in [tags] {
    translate {
      source            => "station_id"
      target            => "station_timezone"
      dictionary_path   => "/etc/logstash/station_timezones.csv"
      refresh_interval  => 300
      fallback          => "UTC"
    }
  }
}
